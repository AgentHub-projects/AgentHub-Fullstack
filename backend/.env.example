PORT=3001
AGENT_COMMAND=claude
# Set MOCK_AGENT=true for deterministic local fixtures.
# Unset MOCK_AGENT to validate the real local Claude Code CLI path.
# Verify the CLI first with: Get-Command claude
# MOCK_AGENT=true
AGENTHUB_TEST_REPO_PATH=D:\agent\AgentHub-Test
DATABASE_URL=postgresql://agenthub:agenthub@localhost:5432/agenthub?schema=public
